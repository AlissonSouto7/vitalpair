/**
 * Web adapter of nutrition: {@code NutritionController} and the API's request and response DTOs
 * ({@code LogMealRequest}, {@code DailySummaryResponse}). Converts between HTTP and the
 * application's commands and results, and wraps every response in {@code ApiResponse}.
 */
package com.aps.vitalpair.nutrition.infrastructure.web;
