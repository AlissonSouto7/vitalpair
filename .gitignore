### Maven ###
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

### OS ###
.DS_Store
Thumbs.db

### Merge and patch leftovers ###
*.orig
*.rej

### VitalPair ###
# Environment files hold secrets and are never versioned. See .env.example.
.env
.env.*
!.env.example
# Local AI session context (notes, worklog, local settings). Never versioned.
.claude/
# Phase plans rendered for the owner; the editable source lives in .claude/notes.
docs/planos/
# Logs
*.log
logs/

### Node (repository tooling: husky, commitlint, prettier) ###
node_modules/
# Husky's own generated helper; the hooks themselves are committed.
.husky/_/

# Filled-in environment files live only on the machine: they hold the database password,
# the JWT secret and the AI key. Only the .example versions are committed.
deploy/env/*.env
!deploy/env/*.env.example
deploy/env/.deployed-*
deploy/env/.previous-*
# Basic-auth credentials for the staging extras, written by deploy/scripts/htpasswd.sh.
deploy/env/htpasswd

# Local AI session pointer; the rules it points to are versioned in docs/ENGINEERING.md.
CLAUDE.md

# Manual QA sweeps: a snapshot of what a person saw on a given day, with screenshots of real
# accounts and their data. Useful while the findings are being worked through and misleading
# afterwards, because it describes behaviour the fixes have already changed. The findings that
# survive belong in docs/features/<name>.md, where they are kept up to date.
docs/qa/
