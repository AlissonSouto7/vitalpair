# Same-origin path. Vite proxies /api to the backend (see vite.config.ts), which is
# what the SameSite=Strict refresh cookie needs and what lets one tunnel expose the app.
VITE_API_URL=/api/v1
VITE_GOOGLE_CLIENT_ID=
